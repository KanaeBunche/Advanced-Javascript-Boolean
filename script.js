// Exercise: Boolean Values Expertise

// Task 1:
// Declare a variable `isAdult` and set it to `true` if the age is 18 or older. Otherwise, set it to `false`.


// Task 2:
// Write a boolean expression to check if a number is both greater than 10 and less than 20.


// Task 3:
// Declare a variable `isEven` and set it to `true` if a given number is even. Otherwise, set it to `false`.


// Task 4:
// Write a boolean expression to check if a string is not empty.


// Task 5:
// Declare a variable `canVote` and set it to `true` if the person's age is 18 or older and they are a citizen. Otherwise, set it to `false`.
